# Т-Инвест Lite

Минималистичный нативный Android-клиент для брокера **Т-Инвестиции** — без рекламы
и лишних функций. Только то, что нужно: портфель, котировки с графиком, сделки и
история операций.

Работает поверх официального **T-Invest API** (REST/JSON-шлюз) в режиме
**песочницы**: виртуальные деньги, но реальные рыночные котировки. Это безопасно для
повседневного использования и разработки — реальный счёт не затрагивается.

> Приложение неофициальное и не аффилировано с Т-Банком.

## Возможности

- **Портфель и баланс** — стоимость портфеля, доходность, свободные средства, список позиций.
- **Рынок** — поиск инструментов (акции, облигации, фонды, валюты) по тикеру/названию/ISIN.
- **Инструмент** — последняя цена, лучшие bid/ask, свечной график (1Д / 1Н / 1М / 1Г).
- **Торговля** — рыночные и лимитные заявки на покупку/продажу в песочнице.
- **Операции** — история сделок, комиссий, дивидендов и купонов за 90 дней.
- **Безопасность** — токен хранится в `EncryptedSharedPreferences`, исключён из бэкапов,
  не попадает в логи.

## Технологический стек

| Слой        | Технология                                              |
|-------------|---------------------------------------------------------|
| Язык        | Kotlin 2.0                                              |
| UI          | Jetpack Compose + Material 3 (тёмная тема)              |
| Архитектура | MVVM (ViewModel + StateFlow), репозиторий, ручной DI    |
| Сеть        | Retrofit + OkHttp + kotlinx.serialization              |
| Навигация   | Navigation Compose                                      |
| Хранилище   | androidx.security EncryptedSharedPreferences           |

## Структура проекта

```
app/src/main/java/com/tinvestlite/
├── TInvestApp.kt              # Application + DI-контейнер
├── MainActivity.kt
├── di/AppContainer.kt         # ручной граф зависимостей
├── data/
│   ├── ApiResult.kt           # Success/Error обёртка
│   ├── local/TokenStore.kt    # зашифрованное хранилище токена
│   ├── remote/
│   │   ├── TInvestApi.kt       # Retrofit-интерфейс (REST-шлюз)
│   │   ├── AuthInterceptor.kt  # Bearer-токен
│   │   ├── NetworkModule.kt    # OkHttp + JSON
│   │   └── dto/                # модели запросов/ответов API
│   └── repository/InvestRepository.kt
├── util/Money.kt              # Quotation/MoneyValue ↔ BigDecimal, форматирование
└── ui/
    ├── theme/                 # цвета, типографика, тема
    ├── navigation/            # маршруты + NavHost
    ├── common/                # переиспользуемые компоненты
    ├── onboarding/            # ввод токена
    ├── home/                  # экран с нижней навигацией
    ├── portfolio/             # портфель
    ├── market/                # поиск инструментов
    ├── instrument/            # котировки + свечной график
    ├── trade/                 # выставление заявки
    ├── operations/            # история операций
    └── settings/              # пополнение песочницы, выход
```

## Сборка

Требуется **Android Studio** (Ladybug или новее) либо Android SDK с `compileSdk 35`.

```bash
./gradlew assembleDebug      # debug-APK → app/build/outputs/apk/debug/
./gradlew installDebug       # установить на подключённое устройство/эмулятор
```

Открой проект в Android Studio и нажми **Run** — это самый простой путь.

## Как получить API-токен

1. Зайди на `t-invest.tinkoff.ru` (веб-кабинет Т-Инвестиций).
2. **Настройки → Токены для T-Invest API → создать токен**.
3. Скопируй токен и вставь его на экране входа в приложении.

Токен можно отозвать в том же разделе в любой момент.

## Заметки по реализации

- API-методы вызываются через публичный REST-шлюз
  `https://invest-public-api.tinkoff.ru/rest/<package>.<Service>/<Method>`.
- Шлюз сериализует поля `int64` (`units`, `quantity`, …) как строки в кавычках,
  поэтому JSON-парсер настроен в режиме `isLenient`.
- Счёт песочницы создаётся автоматически при первом входе; пополнить его виртуальными
  деньгами можно на вкладке «Настройки».

## Дальнейшие идеи

- Перевод торговли на боевой счёт (OrdersService) с дополнительным подтверждением.
- Стрим котировок через gRPC/WebSocket вместо опроса.
- Уведомления о достижении целевой цены, виджет портфеля.
